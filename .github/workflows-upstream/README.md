# Archived upstream workflows

These workflows are retained for reference and upstream synchronization, but they are intentionally outside `.github/workflows` so GitHub Actions does not schedule them in this fork.

The only active workflow is `.github/workflows/muse-server-image.yml`, which publishes the CUDA 13 `llama-server` image for Linux amd64 on `v*` tags or manual dispatch.
